# ── Anthropic ──────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxx

# ── Authentification API ────────────────────────────────────────────────────
# ⚠️  Générez une valeur unique AVANT tout déploiement — ne réutilisez pas cet exemple.
#     Commande : python -c "import secrets; print(secrets.token_urlsafe(32))"
# ⚠️  L'exemple ci-dessous est PUBLIC (déposé sur GitHub) — il ne protège rien.
API_KEY=qU6kGFWrHCh_AEYDVlgf03_A2I1EJPanKcAR7OKP4qQ

# ── PostgreSQL ───────────────────────────────────────────────────────────────
# ⚠️  Générez un mot de passe unique AVANT tout déploiement — ne réutilisez pas cet exemple.
#     Commande : python -c "import secrets; print(secrets.token_urlsafe(32))"
# ⚠️  L'exemple ci-dessous est PUBLIC (déposé sur GitHub) — il ne protège rien.
POSTGRES_DB=ragdb
POSTGRES_USER=raguser
POSTGRES_PASSWORD=AYwF8hEDgMvy9kSTuNxCls6y8bUY-J2pPt5D_44j6As

# ── CORS (optionnel, production) ─────────────────────────────────────────────
# Décommentez et adaptez pour restreindre les origines autorisées.
# CORS_ORIGINS=https://mon-app.exemple.com

# ── Environnement ────────────────────────────────────────────────────────────
# En production, API_KEY et POSTGRES_PASSWORD (>= 16 car.) sont obligatoires.
# ENVIRONMENT=production
